const fs=require('fs'); const s=fs.readFileSync('server.js','utf8'); let stack=[]; for(let i=0;i<s.length;i++){ const ch=s[i]; if(ch==='(') stack.push(i); else if(ch===')'){ if(stack.length) stack.pop(); else console.log('Unmatched ) at',i+1); }} if(stack.length){ console.log('Unmatched ( count',stack.length); for(let pos of stack.slice(-10)){ const start=Math.max(0,pos-40); const end=Math.min(s.length,pos+40); console.log('pos',pos+1); console.log(s.slice(start,end)); console.log('---'); }} else console.log('no unmatched (');
