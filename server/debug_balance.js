const fs=require('fs'); const lines=fs.readFileSync('server.js','utf8').split(/\r?\n/); let par=0, br=0, sq=0; for(let i=0;i<lines.length;i++){ const line=lines[i]; let oldPar=par, oldBr=br, oldSq=sq; for(let ch of line){ if(ch==='(') par++; else if(ch===')') par--; else if(ch==='{') br++; else if(ch==='}') br--; else if(ch==='[') sq++; else if(ch===']') sq--; } if(par!==oldPar || br!==oldBr || sq!==oldSq){ console.log(`${i+1}: par=${par} br=${br} sq=${sq} | ${line.trim()}`); } } console.log('FINAL',par,br,sq);