const fs=require('fs'); const s=fs.readFileSync('server.js','utf8'); const idx=s.indexOf("app.get('/products'"); if(idx===-1){console.log('not found'); process.exit(0);} let par=0, br=0; let i=idx; for(;i<s.length;i++){ const ch=s[i]; if(ch==='(') par++; else if(ch===')') par--; else if(ch==='{') br++; else if(ch==='}') br--; if(par===0 && br===0){ console.log('block ends at char', i+1); const before=s.slice(Math.max(0,i-200), i+200); console.log(before); break; } } console.log('done');